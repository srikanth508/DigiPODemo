import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ShipViaMasterComponent } from './ship-via-master.component';

describe('ShipViaMasterComponent', () => {
  let component: ShipViaMasterComponent;
  let fixture: ComponentFixture<ShipViaMasterComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ShipViaMasterComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ShipViaMasterComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
