import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TenderDashDetailsComponent } from './tender-dash-details.component';

describe('TenderDashDetailsComponent', () => {
  let component: TenderDashDetailsComponent;
  let fixture: ComponentFixture<TenderDashDetailsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TenderDashDetailsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TenderDashDetailsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
