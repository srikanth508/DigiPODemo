import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PoOrderItemsComponent } from './po-order-items.component';

describe('PoOrderItemsComponent', () => {
  let component: PoOrderItemsComponent;
  let fixture: ComponentFixture<PoOrderItemsComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PoOrderItemsComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PoOrderItemsComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
